export interface IExperience { // the education 
    exTitle: string,
    exPlace: string,
    exBefore: string,
    exAfter: string,
    exDesc?: string, // optional paramter (contain the bech project )
    exCertificate: string,
    showFields: boolean,
}