export interface IProject { // the education 
    proTitle: string,
    proBefore: string,
    proAfter: string,
    proDesc?: string, // optional paramter (contain the bech project )
    proLink: string,
    showFields:boolean,

}